[extend]
useDefault = true

[allowlist]
description = "Exempt operational-test harness and runbook: contain deliberately fake redaction-test fixtures, not real secrets."
paths = [
  '''docs/optest\.js''',
  '''docs/OPERATIONAL_TEST\.md''',
]
